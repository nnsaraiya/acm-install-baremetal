# username/password are base64 of "admin"/"password" (the sushy-tools
# credentials set up in the runbook's sushy-tools step). Change both the
# htpasswd entry there and these values together if you want different
# credentials:
#   printf '%s' <user> | base64 -w0
#   printf '%s' <password> | base64 -w0
apiVersion: v1
kind: Secret
metadata:
  name: spoke-bmc-secret
  namespace: spoke
type: Opaque
data:
  username: YWRtaW4=
  password: cGFzc3dvcmQ=

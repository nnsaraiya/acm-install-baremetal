# Generate with: jq -c . ~/.openshift/pull-secret.json | base64 -w0
apiVersion: v1
kind: Secret
metadata:
  name: pull-secret
  namespace: spoke
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: REPLACE_PULL_SECRET_B64
